# This variant assumes the admin origin is reachable only through Cloudflare Access
# (for example via cloudflared tunnel or firewall-restricted ingress).
# Without origin restriction this header can be spoofed and is not sufficient on its own.
if ($http_cf_access_authenticated_user_email = "") {
    return 403;
}
